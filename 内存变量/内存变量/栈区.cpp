#include<iostream>
using namespace std;
//int* func()
//{
	//int a = 10;//局部变量 存在栈区，栈区的数据在执行完后自动释放
	//return &a;//返回局部变量的位置

//}
int main() {
	//接受func函数的返回值
	//int * p = func();

	//cout << *p << endl;//第一次可以打印正确数字是因为编译器进行了保留（g++不可使用）
	//cout << *p << endl;//第二次因为变量已经释放所以无法打印正确数字

	system("pause");

	return 0;
}